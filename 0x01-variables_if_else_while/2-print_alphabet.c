#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: Printing all lower case characters
 * Return: success
 */
int main(void)
{i
	for (char lower_Case = 'a'; lower_Case <= 'z'; lower_Case++);
	putchar(lower_Case);
}
return (0);
}

