#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: Printing all lower case characters
 * Return: Zero is success
 */
int main(void)
{
	char lower_Case = 'a';

	while (lower_Case <= 'z');
{
	putchar(lower_Case);
	lower_Case++;
}
return (0);
}

